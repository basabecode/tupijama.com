# Ejemplo de variables de entorno para este proyecto
# Copia este archivo a .env.local y/o añade las variables en Vercel (Settings → Environment Variables)

# URL pública de Supabase (usada en cliente y servidor)
# Ejemplo de variables de entorno para este proyecto
# Copia este archivo a .env.local (en desarrollo) y/o añade las variables en Vercel (Settings → Environment Variables)

# URL pública de Supabase (ejemplo)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Clave anónima (solo pública) usada por el cliente
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (solo en servidor: no exponer en cliente ni en .env cliente; usar en Vercel como "Environment Variable" marcada como "Secret")
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Otras variables opcionales que el proyecto podría usar
# NEXTAUTH_URL=http://localhost:3000
# NEXT_PUBLIC_STRIPE_KEY=pk_test_...
# Admin secret temporal para endpoints backend (opcional)
ADMIN_SECRET=your-admin-secret
